--- a/schema_tmg.sql
+++ b/schema_tmg.sql
@@ -150,7 +150,8 @@ CREATE TABLE IF NOT EXISTS memory_changes (
 
 ALTER TABLE memory_changes
 ADD CONSTRAINT memory_changes_memory_id_fkey
-FOREIGN KEY (memory_id) REFERENCES memories(id);
+FOREIGN KEY (memory_id) REFERENCES memories(id)
+ON DELETE CASCADE;  -- SECURITY FIX: Enable cascade delete for GDPR compliance
 
 -- ============================================================================
 -- CONFLICT DETECTION
@@ -180,10 +181,12 @@ CREATE TABLE IF NOT EXISTS memory_conflicts (
 
 ALTER TABLE memory_conflicts
 ADD CONSTRAINT memory_conflicts_memory_a_id_fkey
-FOREIGN KEY (memory_a_id) REFERENCES memories(id);
+FOREIGN KEY (memory_a_id) REFERENCES memories(id)
+ON DELETE CASCADE;  -- SECURITY FIX: Enable cascade delete
 
 ALTER TABLE memory_conflicts
 ADD CONSTRAINT memory_conflicts_memory_b_id_fkey
-FOREIGN KEY (memory_b_id) REFERENCES memories(id);
+FOREIGN KEY (memory_b_id) REFERENCES memories(id)
+ON DELETE CASCADE;  -- SECURITY FIX: Enable cascade delete
 
 -- ============================================================================
